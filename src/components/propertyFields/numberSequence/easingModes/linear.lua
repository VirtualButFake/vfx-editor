return {
    In = function(a, b, t)
        return a + (b - a) * t
    end,
    Out = function(a, b, t)
        return a + (b - a) * t
    end,
    InOut = function(a, b, t)
        return a + (b - a) * t
    end,
}